{% extends 'layouts/base.twig' %}

{% block content %}

<main class="container-fluid">
  <div class="row mainGrid">
    <button id="desktopMenu" class="hamburger d-none d-lg-block">
      <span class="hamburger__line"></span>
      <span class="hamburger__line"></span>
      <span class="hamburger__line"></span>
      <span class="visually-hidden">Menu</span>
    </button>
    <aside id="desktopNav" class="sidebar">
      {% include 'partials/nav.twig' %}
    </aside>
    <div class="container">
      {% if not fn('is_front_page') %}
        <div class="row">
          <nav class="col-12 col-md-10">
            {{ fn('breadcrumb') }}
          </nav>
        </div>
      {% endif %}
      <div class="row">
        <div class="col-12 col-md-10 mt-5 content">
          <h1>{{ post.title }}</h1>
          {{ post.content }}
          {% if post.parent.title == "Leksjoner" %}
            {% include 'partials/children.twig' %}
          {% elseif post.title == "Leksjoner" %}
            {% include 'partials/courses.twig' %}
          {% else %}
            {% include 'partials/pagination.twig' %}
          {% endif %}

        </div>
      </div>
    </div>
  </div>
</main>

<aside class="tableOfContents">
  <ul id="tableOfContents" class="tableOfContents__list">
  </ul>
</aside>
<button id="tableOfContentsButton" class="tableOfContents__button">Vis innhold</button>
{% endblock %}